{
  "mcpServers": {
    "ai-router": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/server.py"],
      "env": {
        "AI_ROUTER_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "AI_ROUTER_STATE_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
